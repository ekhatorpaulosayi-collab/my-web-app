// Feature flags for receipt functionality

// Hides receipt options in Business Settings UI only
export const RECEIPT_SETTINGS_ENABLED = false;

// Still allow building/sending receipts from Sales History, EOD, and manual buttons
export const RECEIPT_SENDING_ENABLED = true;
