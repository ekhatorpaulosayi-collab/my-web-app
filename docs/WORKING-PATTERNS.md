# Working Patterns — Paul's Storehouse Development
_30-day snapshot: May 1–30, 2026_
_Generated by audit at end of Session 9. Not a living document —
this is a point-in-time capture of how Paul actually worked across
75 commits / 14 active days. Re-audit in ~6 months if useful._

---

**75 commits across 14 active days.** Avg commit body 1,175 bytes (you write *long* commit messages — see "writing patterns" below). Net change: +44,236 / −1,460 lines across 188 file-touches.

## Cadence — when you actually work

**Active days (17/30 = 57%):** May 1, 2, 3, 13, 14, 15, 16, 17, 18, 19, 27, 28, 29, 30. **You don't code every day.** Big multi-day pushes separated by 5–10 day quiet periods:

- **May 1–3** (20 commits): Chat widget timestamp/security cleanup
- **(May 4–12: 9-day gap)**
- **May 13–19** (47 commits): Paystack subaccount foundation + KYC v1 build
- **(May 20–26: 7-day gap)**
- **May 27–30** (8 commits): F3 fee math + Cart Pay-with-Card + units migration

**Most productive day:** May 16 — **17 commits in one day**. That's the KYC backend slice (steps 1a–6) you described as "Session 4 ended at step 6" in CLAUDE.md. You shipped 6 RPCs, 4 tables, 2 docs, the reviewer scripts, and the F2/F3 tier guards in a single sitting.

**Most productive hour:** 6 AM (14 commits across the month). Second: 22:00 (6 commits). You front-load and tail-end the day. **Almost nothing between 09:00 and 14:00** — only 1 commit in that window all month. That looks like the work-from-home pattern: deep focus pre-breakfast + late evening after family hours.

## The shape of a Storehouse work day

Looking at May 16 (the 17-commit day) and May 30 (today, 5 commits) as case studies:

- **May 16**: Started 01:29 with a spec-fix commit, slept ~02:00–06:00 (no commits), resumed 06:17 and shipped steady through 17:36. **Two distinct sessions** in one calendar day separated by sleep.
- **May 30**: 06:42, 07:23, 07:31 (early Cart fixes), then nothing for 14 hours, then 22:03, 22:47, 23:16 (the units fix + payment_method work in this session). **Same two-session pattern.**

You don't binge for 16-hour stretches. You do 2 focused 3–4 hour blocks.

## What you worked on

Top files touched (proxy for where attention went):

| File | Touches | What it represents |
|---|---|---|
| **CLAUDE.md** | 10 | Operational rules + session log accretion |
| `src/locales/strings.json` | 9 | i18n work (probably the KYC wizard text) |
| `initiate-storefront-payment/index.ts` (F3) | 8 | The fee-math battle — Phases 2–8 of Paystack |
| `docs/KYC_V1_SPEC.md` | 8 | Spec-driven KYC build |
| `create-paystack-subaccount/index.ts` (F2) | 6 | KYC adjacent |
| `ConversationsSimplifiedFixed.tsx` | 6 | The TIMESTAMPTZ migration aftermath |
| `Cart.tsx` | 5 | This week's Pay-with-Card work |
| `App.jsx` | 4 | Sales recording + payment_method fixes |

**Theme:** payments infrastructure. ~50 of 75 commits (67%) are direct payments work (`feat(payments)`, `fix(cart)`, `feat(paystack)`, `feat(kyc)`, `fix(F3)`, `fix(sales)`). KYC is the dominant feature build, F3 fee math is the dominant bug-fight, units migration was today's atomic correction.

## How you commit

**Conventional-commit discipline is strong but inconsistent.** Prefixes seen:
- 33 `feat` ✓ (lowercase, scope-tagged: `feat(payments):`, `feat(kyc):`)
- 11 `fix` ✓
- 8 `docs`
- 7 `FIX:` ← shouting variant from early May, you transitioned away from this style around May 13
- 3 `chore`
- 1 each `STABLE:`, `SECURITY:`, `DIAGNOSTIC:` ← these are old-style; haven't appeared since May 2

**Inflection point: May 13.** That's when you switched from `FIX:`/`STABLE:` shouting to lowercase conventional-commits with scopes. Coincides with the start of the Paystack subaccount work — probably because that was a bigger, more disciplined feature build that demanded better hygiene.

**Commit message bodies are unusually long.** Average 1,175 bytes (~200 words). The longest was 4,223 bytes (~700 words). Modern norm is 1–3 sentence bodies; you write essays. The bodies typically include:
- What changed and why
- What is intentionally *out of scope*
- Forward references to deferred items

Today's units fix commit is a perfect example: 8-paragraph body, explicit "out of scope" section listing 4 separate tickets. This is **excellent for future-you and other reviewers**, but it does mean your `git log -p` is denser than most.

## Self-discipline patterns

I searched commit messages for scope-control language. **12 commits in 30 days explicitly cite "deferred / out of scope / next session / TODO"** — about 16% of all commits. Examples:
- `docs(kyc): defer step 7 (email notification) to Phase 1.5`
- `feat(kyc): step 5 - F3 velocity override lookup (defer single-txn enforcement to v1.5...)`
- `chore(payments): file TODO(prevent-orphan-subaccount) on F2`

You **routinely choose to ship narrower scope rather than expand**. This is a learned skill — not the default for most developers. The CLAUDE.md "Out of scope" sections in 3 commits from today alone confirm it's habitual now.

## Branch hygiene

10 feature/fix branches still exist locally and on origin, all merged (verified: `git branch --merged main` would include them). You're not deleting branches after merge:

```
2026-05-30  fix/sales-mapper-paymentmethod-alias  ← today
2026-05-30  fix/payment-method-case-insensitive   ← today
2026-05-30  feat/sales-units-to-kobo              ← today
2026-05-30  fix/paystack-amount-param             ← today
2026-05-30  fix/paystack-email-param              ← today
2026-05-29  fix/paystackpop-key                   ← yesterday
2026-05-29  feat/cart-pay-with-card               ← yesterday
2026-05-29  feat/f3-transaction-charge-fix        ← yesterday
2026-05-19  feat/kyc-wizard-v1                    ← 11 days stale
```

**Small thing worth fixing:** these are cleanup candidates. Not urgent, but the older you let them accumulate the more confusing the branch list gets when you `git branch -a` from a fresh session.

## Documentation discipline

10/75 commits (13%) touched `docs/`. You write docs *alongside* code, not after:
- `docs/KYC_V1_SPEC.md` updated 8 times across the KYC build (spec-as-you-go)
- `docs/PAYSTACK-DEBUG.md` updated 4 times (session-by-session payment history)
- `docs/SESSION_4_LESSONS_CAPTURED.md`, `docs/CODEBASE_MAP.md` — pure meta-docs about the process itself

**Two distinct doc patterns:**
1. **Spec docs** (KYC_V1_SPEC.md): written upfront, revised as reality diverges
2. **Session logs** (PAYSTACK-DEBUG.md, CLAUDE.md session sections): written post-hoc as historical record

This is mature documentation practice. Most solo devs do neither.

## Notable behaviors

1. **Atomic-fix discipline.** Today's units fix shipped DB migration + 3 frontend changes + edge function in a single commit (`d1d094b`). You explicitly cited atomicity in the commit body. You did the same on May 27 (`4f7489b`, the dual-write commit covering 2 systems).

2. **You name your decisions.** "D1: Customer absorbs Paystack fee. D2: Vendor absorbs Storehouse fee. D3: Listed product price is clean..." (from CLAUDE.md Session 7). You commit-message decisions so they don't get re-litigated. This is rare and good.

3. **You spike before shipping.** The F3 fee math work has 7 documented charge references in CLAUDE.md (`zqd488sxbshceww`, `tqi0t3rm6nelg9i`, etc.) — each one a manual test against test-mode Paystack. You don't ship money-flow code without verifying against the real API.

4. **You file follow-up bugs as you find them.** Every "out of scope" list in a commit body is a future ticket you've registered against yourself rather than letting the surrounding cleanup creep into the current PR.

5. **You re-investigate your own work when reality contradicts it.** Session 8's whole point was correcting Session 7's wrong interpretation of Paystack's `bearer="subaccount"` behavior. You didn't defend the old position; you ran spikes, found the truth, fixed the code, and updated the docs.

## Two patterns worth examining

**Doc accretion in CLAUDE.md.** It's now ~750 lines, ~80% session log. Each session adds; nothing gets compressed. Sessions 1–6 are still there even though the work has moved well past them. The earlier-prompt-tonight proposal to split this into a layered structure (slim orchestrator + topic docs + rolling session log) was a direct response to this drift. **You felt it; you proposed the fix; you stopped before drafting.** Healthy instinct.

**Gap days.** The 9-day gap (May 4–12) and 7-day gap (May 20–26) coincide with no commits at all — not even tiny docs. This *could* be context-switching to non-coding work (research, planning, customer conversations) or it could be burnout-recovery from the May 1–3 and May 13–19 bursts. Worth noticing for your own pacing: you burst then recover, you don't sustain.

## What this tells me about working with you

- **Brief me thoroughly upfront.** You write 1,000-byte commit messages because you value context preservation. Same applies to instructions to me — your multi-phase prompts (PRE-FLIGHT / PHASE 1 / PHASE 2 / INTEGRITY GATES / STOP BEFORE COMMIT) are the same artifact.
- **Stop-before-commit is your default mode.** I should default to assuming you'll review before commit unless you explicitly say otherwise.
- **Audit-first before action.** Today's prompt sequence was: investigate → diff review → apply → re-verify → commit → push → smoke-check failure → investigate → fix. You don't skip the verify steps.
- **Atomic commits with explicit out-of-scope.** Match the existing pattern.
- **Money flow gets extra care.** F3 has 8 commits across the month for a reason. Don't assume my fix worked — show spike evidence.
